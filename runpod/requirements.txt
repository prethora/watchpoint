# WatchPoint RunPod Inference Worker - Python Dependencies
# Used by the GPU-accelerated inference worker deployed on RunPod Serverless.

# RunPod SDK
runpod>=1.6.0

# Data Science Stack (shared with eval worker)
numpy>=1.24.0
xarray>=2023.0.0
zarr>=2.16.0
numcodecs>=0.11.0
s3fs>=2023.6.0
pandas>=2.0.0

# ML Inference (Earth-2 Studio)
# earth2grid is installed separately in the Dockerfile (not on PyPI,
# requires CUDA to be hidden during build to skip C++ extensions).
earth2studio[atlas,stormscope]==0.12.1
scipy>=1.11.0

# Infrastructure
boto3>=1.28.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Local Simulator (FastAPI)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
httpx>=0.24.0
